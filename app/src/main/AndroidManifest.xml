<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.kelompok5.mitahara">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Mitahara">

        <activity android:name=".AddDietPlanActivity"/>
        <activity android:name=".DietPlanDetailActivity" />
        <activity android:name=".HomePage" />
        <activity android:name=".home_article_1" />
        <activity android:name=".SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".welcome_page"/>
        <activity android:name=".create_new_password" />
        <activity android:name=".reset_pass_verif" />
        <activity android:name=".reset_password" />
        <activity
            android:name=".ui.login.signin"
            android:label="@string/title_activity_signin" />
        <activity android:name=".MainActivity" />

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>